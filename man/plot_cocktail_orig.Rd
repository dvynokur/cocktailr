% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cocktail_orig.R
\name{plot_cocktail_orig}
\alias{plot_cocktail_orig}
\title{Original Cocktail dendrogram (reference plot; minimal changes)}
\usage{
plot_cocktail_orig(x, highquality = 1, file = "Cocktail_dendrogram.png")
}
\arguments{
\item{x}{A result list from \code{\link[=cocktail_cluster]{cocktail_cluster()}} or \code{\link[=cocktail_cluster_orig]{cocktail_cluster_orig()}}
containing \code{Cluster.species}, \code{Cluster.merged}, and \code{Cluster.height}.}

\item{highquality}{Integer: 1 uses a large PNG device (Cairo if available),
0 draws to the current device. Default 1.}

\item{file}{Path to the output PNG when \code{highquality == 1}. Ignored otherwise.}
}
\value{
Invisibly returns a list with:
\itemize{
\item \code{Species.sort} — character vector used to order species labels
\item \code{Cluster.position} — matrix of segment coordinates
}
}
\description{
Draw the Cocktail dendrogram using the plotting approach from Appendix S2
(Bruelheide 2016) with only minimal wrapper code. This mirrors the original:
species order via the "Species.sort" binary sequence and manual leg/crossbar segments.
}
\details{
If \strong{Cairo} is installed, the function uses the original \code{Cairo::Cairo()} call.
Otherwise it falls back to \code{grDevices::png()} with the same pixel dimensions.
The plotting logic itself is kept as in the original code block.
}
\examples{
\donttest{
  m <- as.matrix(vegan::dune); m[m > 0] <- 1L
  ref <- cocktailr::cocktail_cluster(m, progress = FALSE)
  plot_cocktail_orig_min(ref, highquality = 0)
}

}
\seealso{
\code{\link[=cocktail_cluster]{cocktail_cluster()}}, \code{\link[=cocktail_cluster_orig]{cocktail_cluster_orig()}}
}
