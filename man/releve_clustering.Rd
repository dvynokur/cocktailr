% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/releve_clustering.R
\name{releve_clustering}
\alias{releve_clustering}
\title{Hierarchical clustering of relevés based on shared Cocktail clusters}
\usage{
releve_clustering(
  x,
  phi_cut = 0.2,
  dist_method = "manhattan",
  clust_method = "average",
  dist_engine = c("base", "parallelDist"),
  hclust_engine = c("stats", "fastcluster"),
  par_cores = NULL
)
}
\arguments{
\item{x}{A Cocktail object (from \code{cocktail_cluster_mod()} or \code{cocktail_cluster()}).}

\item{phi_cut}{Numeric. Keep only clusters with height ≥ \code{phi_cut}. Default = 0.2.}

\item{dist_method}{Character. Distance for plots (default "manhattan").}

\item{clust_method}{Character. Linkage for hierarchical clustering (default "average").}

\item{dist_engine}{"base" (stats::dist) or "parallelDist" (parallelDist::parDist). Default "base".}

\item{hclust_engine}{"stats" (stats::hclust) or "fastcluster" (fastcluster::hclust). Default "stats".}

\item{par_cores}{Integer or NULL. Cores for parallelDist; NULL lets parallelDist choose.}
}
\value{
A list of class \code{"releve_hclust"} with:
\itemize{
\item \code{hclust}         : the hclust object
\item \code{dist}           : the distance object used
\item \code{kept_clusters}  : indices of clusters retained (after φ cut & dedup)
\item \code{plot_labels}    : plot names
\item \code{engines}        : list(dist_engine, hclust_engine)
}
}
\description{
Builds a hierarchical clustering (\code{hclust}) of plots from shared cluster
memberships (\code{Plot.cluster}). Steps:
\enumerate{
\item Keep clusters with φ >= \code{phi_cut}.
\item Remove redundant cluster columns with identical plot membership (keep last).
\item Compute distances (optionally with parallelDist).
\item Agglomerate (optionally with fastcluster).
}
}
