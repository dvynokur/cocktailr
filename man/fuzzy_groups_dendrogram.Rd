% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_fuzzy.R
\name{fuzzy_groups_dendrogram}
\alias{fuzzy_groups_dendrogram}
\title{Dendrogram of fuzzy Cocktail groups (groups as tips)}
\usage{
fuzzy_groups_dendrogram(
  fuzzy,
  cut,
  min_phi = 0,
  dist = c("bray", "cosine"),
  hclust_method = "average",
  group_names = NULL,
  pdf_file = NULL,
  width_in = NULL,
  height_in = 7
)
}
\arguments{
\item{fuzzy}{data.frame from cocktail_fuzzy(); columns must include:
group, species, phi, cut}

\item{cut}{numeric phi cut to use (e.g. 0.30)}

\item{min_phi}{keep only memberships with phi >= min_phi (default 0)}

\item{dist}{distance between groups: "bray" (default) or "cosine"}

\item{hclust_method}{linkage for hclust: "average" (UPGMA), "complete", etc.}

\item{group_names}{optional named character vector mapping group codes to nice names
e.g. c(G107="Stipa lessingiana grp", G114="Filipendula grp")}

\item{pdf_file}{optional path; if given, plot is written to this PDF}

\item{width_in, height_in}{PDF size if pdf_file is used (width auto-scales if NULL)}
}
\value{
list(hclust=, dist=, X=matrix, labels=character)
}
\description{
Dendrogram of fuzzy Cocktail groups (groups as tips)
}
