% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_releves_new.R
\name{assign_releves_new}
\alias{assign_releves_new}
\title{Assign relevés (plots) to k groups from a releve_clustering result
with optional one-step child level (next branch division).}
\usage{
assign_releves_new(x, k, next_level = FALSE)
}
\arguments{
\item{x}{Object returned by \code{releve_clustering()} (class "releve_hclust").}

\item{k}{Integer, number of fine groups to cut the tree into (parent level).}

\item{next_level}{Logical; if TRUE, also compute the immediate children
of each parent cluster (split each parent once into its two branches).}
}
\value{
A list with:
\itemize{
\item \code{groups}: data.frame(plot, cluster, cluster_orig, parent_cluster, child_cluster)
(parent_cluster/child_cluster are included only if next_level=TRUE)
\item \code{sizes} : table of fine cluster sizes
\item \code{k}     : number of groups used
\item \code{hclust}: the hclust object (for convenience)
\item \code{child_map} : (when next_level=TRUE) integer vector mapping child_id -> parent_id
}
}
\description{
Assign relevés (plots) to k groups from a releve_clustering result
with optional one-step child level (next branch division).
}
